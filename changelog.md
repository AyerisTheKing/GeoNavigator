# История изменений

## v14.8 (UI: Profile Visual Enhancement)

**Дата:** 01.02.2026

### 🎨 UI/UX

- **Profile Title**: Добавлен заголовок "Профиль" в шапке модального окна.
- **Profile Icon**: Добавлена статичная иконка пользователя с градиентом и свечением (без анимации).
- **Layout**: Никнейм и класс теперь расположены на одной линии по центру.
- **Login**: Логин отображается снизу мелким шрифтом (`.profile-login-small`).
- **Gradient Buttons**: Кнопки "История игр" и "Таблица лидеров" получили переливающиеся градиенты в стиле Cyberpunk:
  - Градиент `135deg` (синий → голубой)
  - Анимация `gradientShift` (движение градиента)
  - Эффект `shine` (бегущий блик света)
  - При наведении: увеличение яркости градиента + тень
- **Aesthetics**: Улучшена визуальная иерархия элементов профиля.
- **Fix**: Исправлена кнопка закрытия профиля (использует правильный обработчик событий).

## v14.7 (UI: Profile Redesign - Text Only)

**Дата:** 31.01.2026

### 🎨 UI/UX

- **Profile Modal**: Полностью переверстано модальное окно профиля.
- **Minimalist Design**: Убраны все иконки и смайлики из кнопок (строгий текстовый стиль).
- **Structure**: Новая структура: кнопка закрытия (крестик) → Информация (Никнейм, Класс, Логин) → 3 кнопки действий (История игр, Таблица лидеров, Выйти).
- **Safety**: Убраны обновления статистики (`statFullGames`, `statFullCorrect`, `statFullWrong`) из методов `openStatsModal` и `updateProfileStatsUI` для предотвращения ошибок `null pointer`.
- **Code**: Обновлены ID элементов: `profileDisplayNickname` → `profileNickname`, `profileDisplayLogin` → `profileLogin`, добавлен `profileClass`.
- **Handlers**: Добавлены обработчики для новых кнопок `btnHistory`, `btnLeaderboard`, `btnLogout`.

## v14.5 (Patch: Extreme Difficulty Stats Fix)

**Дата:** 31.01.2026

### 🛠 Исправлено

- **Statistics**: Исправлена проблема сохранения статистики для сложности "Extreme" в Supabase.
- **Validation**: Добавлен whitelist допустимых сложностей `['easy', 'normal', 'hard', 'extreme']` в методе `saveGameStats`.
- **Fallback**: Добавлена защита от ошибок: если сложность не распознана, статистика сохраняется в колонку `stat_normal`.
- **Database**: Метод теперь корректно формирует имя колонки `stat_extreme` для сложности "Extreme".

## v14.4 (Feature: Expanded Leaderboard Columns)

**Дата:** 31.01.2026

### 🏆 Таблица Лидеров

- **UI Update**: Добавлены две новые колонки:
  - **Рекорд**: Максимальный счет за одну игру (Global Best Score).
  - **Игр**: Общее количество сыгранных игр (Total Games).
- **Grid**: Сетка расширена до 6 колонок (`40px 1fr 60px 60px 60px 50px`).
- **Footer**: Липкий футер ("Мой результат") обновлен, чтобы соответствовать новой сетке и отображать новые данные.

## v14.3 (UI: Leaderboard Detail & Redesign)

**Дата:** 31.01.2026

### 🏆 Таблица Лидеров

- **UI/UX**: Переработан внешний вид Таблицы Лидеров. Переход от простой таблицы к детальному списку (Rich List).
- **Grid**: Сетка изменена на 4 колонки: Место | Инфо | Очки | Время.
- **Инфо**: Новая колонка "Инфо" содержит никнейм и логин пользователя.
- **Smart Class**: Бейдж класса скрывается при активном фильтре по классу и показывается в общем списке.

## v14.1 (Patch: Bug Fix)

**Дата:** 31.01.2026

### 🛠 Исправлено

- **History**: Исправлена кнопка "Назад" в окне истории игр. Ранее она не работала, если история была пустой.
- **Navigation**: Теперь кнопка корректно возвращает пользователя в окно профиля.

## v14.0 (System: Decimal Versioning)

**Дата:** 31.01.2026

### 🔄 Система Версий

- **Correction**: Версия исправлена и округлена до v14.0 для соответствия десятичной системе (Float).
- **Rules**: Утверждено правило: Patch +0.1, Feature +0.3.

## v13.15.4 (Architecture: Server-Side Triggers)

**Дата:** 31.01.2026

### 🛠 Архитектура (Registration Flow)

- **Atomic Registration**: Полный переход на триггерную схему регистрации.
- **Optimization**: Убран лишний `.update()` запрос; теперь метаданные (`nickname`, `class_name`, `role`) передаются прямо в `signUp`.
- **Logic**: Метод `performRegister` полностью переписан для поддержки атомарной транзакции создания пользователя.

## v13.15.1 (Patch: CSS Fixes)

**Дата:** 31.01.2026

### 🛠 Исправлено (CSS)

- **Style**: Исправлена ошибка `tap-highlight-color` в `style.css`.
- **Compatibility**: Добавлено стандартное свойство `appearance: none` для слайдеров и переключателей.

## v13.15.0 (Audit: Global Stats Sync)

**Дата:** 31.01.2026

### 🛠 Исправлено (Critical Data Sync)

- **Global Stats**: Исправлена рассинхронизация глобальной статистики. Теперь поля `total_games`, `total_correct`, `best_score`, `total_time` и `region_stats` корректно загружаются из облака при входе и сохраняются обратно после каждой игры.
- **Data Integrity**: Исключена потеря прогресса игроков при смене устройства (ранее статистика могла оставаться только в локальном кеше).

## v13.14 (Patch: Critical Registration Fix)

**Дата:** 31.01.2026

### 🛠 Исправлено (Critical Bug)

- **Registration**: Исправлена критическая ошибка регистрации (Constraint Violation: NULL nickname).
- **Metadata**: Метод регистрации теперь явно передает `nickname` и `class_name` в метаданных, что позволяет триггеру БД корректно заполнить таблицу `profiles`.
- **Validation**: Усилена валидация никнеймов и логинов перед отправкой запроса (предотвращает отправку пустых значений).

## v13.13 (Patch: DB Constraints & Registration Fix)

**Дата:** 31.01.2026

### 🛠 Исправлено (Database & Security)

- **Registration Fix**: Добавлена жесткая проверка на пустые никнеймы и нецензурную лексику перед отправкой запроса.
- **Data Integrity**: Исключена возможность регистрации с `nickname = NULL`.
- **Database**: Подготовлен SQL-скрипт для очистки базы от "безымянных" игроков.

## v13.12 (Feature: Role Hierarchy)

**Дата:** 31.01.2026

### 🛡️ Система Прав (Access Control)

- **Levels**: Введена числовая иерархия ролей (`admin`: 5, `tester`: 3, `teacher`: 1, `user`: 0).
- **Logic**: Проверка прав теперь осуществляется через уровни (`level >= 1`), а не сравнение строк.

## v13.11 (Leaderboard Fix)

**Дата:** 31.01.2026

### 🏆 Таблица Лидеров

- **Вкладки сложности**: Теперь переключение вкладок (Easy/Normal/Hard/Extreme) корректно фильтрует статистику.
- **Подробные данные**: В таблице отображаются очки и время, заработанные именно в выбранном режиме сложности.
- **Локализация**: Вкладки переведены на русский язык.

## v13.10 (UI Patch)

**Дата:** 31.01.2026

### 🎨 UI/UX

- **History**: Заголовок окна истории изменен на **"ИСТОРИЯ ИГР"**.
- **History**: В список сыгранных игр добавлено время сессии (в секундах).

## v13.9 (Hotfix: History Crash)

**Дата:** 31.01.2026

### 🛠 Исправлено

- **History**: Исправлен критический баг, вызывающий ошибку при открытии истории игр ("UI Crash").
- **Fault Tolerance**: Добавлена система безопасного чтения данных истории (поддержка старых и новых форматов записи).
- **Data Protection**: Автоматическое исправление поврежденных записей в массиве `recent_games`.

## v13.8 (UI Update)

**Дата:** 31.01.2026

### 🚀 Новое

- **Profile**: В профиле слева от никнейма теперь отображается **класс** (например, "5-А") в виде стильного бейджа.
- **Viewer**: Геймифицированный бейдж класса также виден при просмотре профилей других игроков.

## v13.7 (Role & Theme Update)

**Дата:** 31.01.2026

### 🚀 Новое (Role Dashboard)

- **Тестер**: Добавлена визуальная **подсветка правильного ответа** (зеленая пульсация) в режиме отладки.
- **UI**: Опция "Разблокировать настройки" заменена на переключатель **"Расширить настройки"**.
- **Темы**: Реализована поддержка тем **Темная** (по умолчанию) и **Светлая**.
- **Settings**: Выбор темы появляется в настройках только при активации опции "Расширить настройки".

### 🛠 Исправлено

- **Role Menu**: Исправлена ошибка, из-за которой кнопки выбора ролей не открывали меню.
- **Modal**: Починена кнопка закрытия (крестик) в модальном окне ролей.

## v13.3 (Patch: Audit Fixes)

**Дата:** 31.01.2026

### 🛠 Исправлено (Fixed)

- **Registration**: Улучшена валидация выбора класса и буквы. Теперь данные отправляются строго в формате "5-Б". Исключена отправка `null` или `undefined` при частичном выборе.
- **Role System**: Добавлена безопасная проверка ролей (`safe check`) для предотвращения ошибок доступа к свойствам `null`.
- **Observer Mode**: Исправлен баг, при котором кнопка "Выйти" могла исчезнуть или появиться некорректно после просмотра чужого профиля. Теперь состояние интерфейса корректно возвращается к состоянию текущего пользователя (Гость или Игрок).
- **Localization**: Добавлены недостающие переводы для полей "Класс", "Буква" и меню ролей.
- **Cleanup**: Удалены отладочные `console.log` из инициализации и музыкального менеджера.

## v13.3

- **UI**: Полностью переработано меню ролей.
- **UI**: Кнопка ролей перемещена в правый верхний угол (Гамбургер-меню).
- **UI**: Новый стиль меню (Neon/Glassmorphism).
- **Code**: Оптимизация обработчиков событий для меню.

## v13.2 (Feature: Roles & Classes)

### Roles & Admin

- **Role System**: Добавлена поддержка ролей `admin`, `teacher`, `tester` (по умолчанию `user`).
- **Role Menu**: Специальное меню для сотрудников (скрыто для обычных игроков).
- **Viewer Mode**: Режим просмотра профиля других игроков для администраторов (Read-Only).

### Registration

- **Class Selection**: Добавлена возможность выбора класса (Цифра + Буква) при регистрации.
- **Database**: Новые поля `role` и `class_name` в таблице `profiles`.

## v12.9 (Patch: Strict Russian Localization)

### Локализация

- **Strict Mode**: "Герметизация" локализации. Убраны технические ключи.
- **Diff**: Сложность теперь: "Легко", "Нормально", "Сложно", "Экстрим".
- **Validation**: Исправлены тексты ошибок и сообщений.

## v12.8 (Patch: Auto-Zoom Logic)

### Геймплей

- **Auto-Zoom**: Исправлена логика зума камеры при ответе.
  - Теперь камера приближает страну и в текстовом режиме (`countryByCapitalText`).
  - Зум полностью отключен в режиме "Самый-самый" (`theMost`).
  - Сохранена зависимость от сложности (Zoom: Hard=True, Extreme=False).

## v12.7 (Feature: Ban System)

### Data

- **SQL**: Добавлена колонка `is_banned` в таблицу `profiles`.
  ```sql
  ALTER TABLE profiles ADD COLUMN IF NOT EXISTS "is_banned" BOOLEAN DEFAULT FALSE;
  ```

### Logic

- **Auth**: Блокировка входа для забаненных пользователей (Script Check).
- **Leaderboard**: Скрытие забаненных игроков из таблицы лидеров.

## v12.5 (Patch: Map UI & Visibility)

### UI

- **Карта**: Скрыты лишние кнопки управления зумом и копирайты для чистоты интерфейса.
- **Метки**: Теперь на сложностях Easy и Normal карта отображает названия стран и городов (Dark All), а на Hard/Extreme она остается "слепой" (Dark NoLabels).

## v12.4

### UI & Config

- **Смена языка**: Блок выбора языка скрыт в настройках.
- **Локализация**: Русский язык теперь установлен принудительно для всех пользователей по умолчанию.

## v12.3 (Hotfix)

### Исправлено

- Исправлена критическая ошибка с `SUPABASE_KEY` (была опечатка в константе).

## v12.2

### Исправлено

- Исправлена ошибка `this.saveStats is not a function`. Метод `saveStats` теперь корректно вызывается из экземпляра класса.

## v12.0 (Major Update)

### База данных

- Полная миграция структуры БД.
- Добавлены JSONB колонки: `stat_easy`, `stat_normal`, `stat_hard`, `stat_extreme`, `recent_games`.
- Добавлена колонка `total_correct`.
- Удалены старые колонки статистики.

### Рефакторинг

- Полностью переписана система сохранения статистики (`saveGameStats`).
- Реализована логика Read-Modify-Write для обновления JSONB данных.
- Исправлен маппинг режимов игры на поля БД.

## v11.9

### Новое

- **Новый режим**: Добавлен режим "Самый-самый" (The Most).
- **Контент**: Добавлена база вопросов для нового режима (`the_most.geo.json`).
- **Геймплей**: Реализована логика вопросов, подсказок и ответов для режима рекордов.

## v11.8 (Security Update)

### Безопасность

- **SQL Constraints**: Добавлены ограничения на уровне БД (email check, min length).
- **Input Validation**: Введена строгая валидация на клиенте (Regex для логина/ника).
- **XSS Protection**: Методы отображения (`innerHTML`) заменены на безопасные (`textContent`) для пользовательских данных.

## v11.5

### UI/UX

- Исправлена навигация в модальных окнах (кнопки "Назад").
- Обновлен дизайн кнопок в профиле.

## v11.2

### Рефакторинг

- Очистка кода от старых комментариев версионности.
- Перенос истории изменений в `CHANGELOG.md`.

## v11.1

### UI

- Обновлена таблица лидеров:
  - Заголовок изменен на "Таблица лидеров".
  - Добавлена кнопка обновления (Refresh).
  - Убран текст "Loading...".

## v11.0

### Features

- **История игр**: Добавлена возможность просмотра последних 10 игр.
- **Профиль**: Отображение ника вместо логина.

## v10.4

### Fixes

- Исправлен баг "Zombie-account" (ошибка при отсутствии профиля).
- Исправлен креш карты (NaN LatLng).
- Исправлена регистрация (конфликт login/nickname).
